{% extends 'base.html.twig' %}

{% block title %}Détails du touite / Touiteur
{% endblock %}

{% block body %}
	<main class="page--default">
		<table class="table">
			<tbody>
				{# <tr>
					<th>Id</th>
					<td>{{ touite.id }}</td>
				</tr> #}
                <tr>
					<th>Auteur</th>
					<td>{{ touite.author }}</td>
				</tr>
				<tr>
					<th>Content</th>
					<td>{{ touite.content }}</td>
				</tr>
				<tr>
					<th>Date</th>
					<td>{{ touite.date ? touite.date|date('H:i · d/m/y') : '' }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('app_touite_index') }}">back to list</a>

		<a href="{{ path('app_touite_edit', {'id': touite.id}) }}">edit</a>

		{{ include('touite/_delete_form.html.twig') }}
	</main>
{% endblock %}
